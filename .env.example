# ---------------------------------------------------------------------------
# Supabase — SDK client (lecture directe depuis le frontend)
# ---------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://votre-projet.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=votre-cle-anon-supabase

# ---------------------------------------------------------------------------
# n8n — Webhook Workflow 1 (Dictation)
# URL complète du webhook versionnné exposé par n8n sur Railway.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_WEBHOOK_N8N_DICTATION_URL=https://votre-instance-n8n.up.railway.app/webhook/v1/dictation

# ---------------------------------------------------------------------------
# n8n — Token d'authentification statique (MVP)
# Valeur partagée entre le frontend (ici) et n8n (variable Railway).
# Ne jamais committer la valeur réelle.
# ---------------------------------------------------------------------------
NEXT_PUBLIC_WEBHOOK_API_TOKEN=votre-token-api-n8n

# ---------------------------------------------------------------------------
# OpenRouter — IA pour generation de pre-devis
# Cle API : https://openrouter.ai/settings/keys
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------
# n8n — Webhook Save Devis (upload Excel vers Google Drive)
# ---------------------------------------------------------------------------
WEBHOOK_N8N_SAVE_DEVIS_URL=https://votre-instance-n8n/webhook/v1/save-devis

AI_API_KEY=sk-or-votre-cle-openrouter
AI_BASE_URL=https://openrouter.ai/api/v1
AI_MODEL=anthropic/claude-3.5-sonnet
